import SpaceXPage from "./Spacex/page";

export default function PostFormPage() {
  return (
    <div >
      <p>
        8. Set up Apollo Client in a Next.js app. Create a Server Component that
        fetches data from a GraphQL API using Apollo Client and passes this data
        to a Client Component, which then displays it on the page.
      </p>
      <SpaceXPage />
    </div>
  );
}
